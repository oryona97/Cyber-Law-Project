This folder is for reusable UI components.

Examples:
- Buttons
- Navigation bars
- Cards
- Modals
- Form inputs

These components should generally be "dumb" or presentational, meaning they receive data via props and render it, without handling too much complex business logic.
